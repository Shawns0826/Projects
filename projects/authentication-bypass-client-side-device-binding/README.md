# authentication-bypass-client-side-device-binding

The published case study for this topic lives in:

`_case_studies/authentication-bypass-client-side-device-binding.md`

Edit that file (or duplicate its pattern for new posts), then commit—**GitHub Actions** rebuilds the Jekyll site for GitHub Pages.

Optional images for this piece can go under `assets/images/` (or a subfolder you prefer) and be referenced with `{{ site.baseurl }}` in Markdown if you switch to HTML, or with normal paths under `/Projects/assets/...` once published.
